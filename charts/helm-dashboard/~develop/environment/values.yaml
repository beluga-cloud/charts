# yaml-language-server: $schema=../../values.schema.json

ingress:
  annotations:
    cert-manager.io/cluster-issuer: selfsigned
  enabled: true
  hostname: helm-dashboard.7f000001.nip.io
  tls: true
