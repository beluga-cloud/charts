---
# Source: helm-dashboard/templates/pvc.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: helm-dashboard
  namespace: "helm-dashboard-validation"
  annotations:
    annotations: enabled
  labels:
    app.kubernetes.io/name: helm-dashboard
    helm.sh/chart: helm-dashboard-1.1.0
    app.kubernetes.io/instance: helm-dashboard
    app.kubernetes.io/managed-by: Helm
    commonLabels01: enabled
    commonLabels02: enabled
spec:
  storageClassName: helm-dashboard-volume
  accessModes:
    - "ReadWriteOnce"
  capacity:
    storage: "20M"
  hostPath:
    path: "/tmp/helm-dasboard"
---
# Source: helm-dashboard/templates/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helm-dashboard
  annotations:
    annotations: enabled
  labels:
    app.kubernetes.io/name: helm-dashboard
    helm.sh/chart: helm-dashboard-1.1.0
    app.kubernetes.io/instance: helm-dashboard
    app.kubernetes.io/managed-by: Helm
    commonLabels01: enabled
    commonLabels02: enabled
spec:
  storageClassName: helm-dashboard-volume
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "20M"
